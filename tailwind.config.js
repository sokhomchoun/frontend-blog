module.exports = {
  mode: 'jit', // Just-In-Time mode (optional but recommended)
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Ensure all React files are included
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
